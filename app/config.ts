/**
 * Default route for the redirection of the user after logout
 */
export const POST_LOGOUT_REDIRECT_ROUTE = "/";

/**
 * Default route for the redirection of the user after login
 */
export const POST_LOGIN_REDIRECT_ROUTE = "/notes";
